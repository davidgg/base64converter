import React from 'react';
import './index.scss';

const Header = () => (
  <header className="Header">
    <nav className="navbar navbar-light bg-light">
      <span className="navbar-brand mb-0 h1">Base64 Converter</span>
    </nav>
  </header>
);

export default Header;
